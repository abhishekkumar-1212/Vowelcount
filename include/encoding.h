#ifndef ENCODING_H
#define ENCODING_H

// Function declarations for encoding operations
void encodeString(const char *input, char *output);
void addParityBit(const char *binary, char *binaryWithParity);

#endif
