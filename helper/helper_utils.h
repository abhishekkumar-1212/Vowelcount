#ifndef HELPER_UTILS_H
#define HELPER_UTILS_H

// Function to print the encoded data (for debugging or logging purposes)
void print_encoded_data(const char *encoded_data);

// Function to check the length of the input string
int get_input_length(const char *input);

#endif // HELPER_UTILS_H
